---
- name: This sets up an httpd and nginx webserver
  hosts: all
  tasks:
  - name: list the version
    command: rpm -qa | egrep -i 'httpd|nginx' 
    register: version
  - debug: msg="status:{{version.stdout_lines}}"
